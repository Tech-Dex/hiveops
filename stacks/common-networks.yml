networks:
  black-sea:
    name: black-sea
    driver: bridge

  nextcloud:
    name: nextcloud
    driver: bridge

  analytics:
    name: analytics
    driver: bridge

  nvr:
    name: nvr
    driver: bridge

  printer:
    name: printer
    driver: bridge